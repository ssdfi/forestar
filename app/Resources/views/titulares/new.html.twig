{% extends 'base.html.twig' %}
{% block title %}{{parent()}} - Nuevo Titular{% endblock %}
{% block body %}
    <h1>Titulare creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('titulares_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
