<div class="col-md-12">
  <div class="row">
    <div class="col-md-4">
      <div class="card">
        <div class="card-header">
          Contable
        </div>
        <div class="card-body">
          <ul>
            {% for item in expediente.historialContables %}
              <li><b>{{item.fechaUltimaModificacion|date('d-m-Y')}}</b>. {{item.observacion}}</li>
            {% endfor %}
          </ul>
        </div>
      </div>
    </div>
    <div class="col-md-4">
      <div class="card">
        <div class="card-header">
          Legales
        </div>
        <div class="card-body">
          <ul>
            {% for item in expediente.historialLegales %}
              <li><b>{{item.fechaUltimaModificacion|date('d-m-Y')}}</b>. {{item.observacion}}</li>
            {% endfor %}
          </ul>
        </div>
      </div>
    </div>
    <div class="col-md-4">
      <div class="card">
        <div class="card-header">
          Foresto Industriales
        </div>
        <div class="card-body">
          <ul>
            {% for item in expediente.historialForestoIndustriales %}
              <li><b>{{item.fechaUltimaModificacion|date('d-m-Y')}}</b>. {{item.observacion}}</li>
            {% endfor %}
          </ul>
        </div>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-md-4">
      <div class="card">
        <div class="card-header">
          Viveros
        </div>
        <div class="card-body">
          <ul>

          </ul>
        </div>
      </div>
    </div>
    <div class="col-md-4">
      <div class="card">
        <div class="card-header">
          Promoción
        </div>
        <div class="card-body">
          <ul>
            {% for item in expediente.historialPromociones %}
              <li><b>{{item.fechaUltimaModificacion|date('d-m-Y')}}</b>. {{item.observacion}}</li>
            {% endfor %}
          </ul>
        </div>
      </div>
    </div>
    <div class="col-md-4">
      <div class="card">
        <div class="card-header">
          SIG
        </div>
        <div class="card-body">
          <ul>
            {% for item in expediente.historialSigs %}
              <li><b>{{item.fechaUltimaModificacion|date('d-m-Y')}}</b>. {{item.observacion}}</li>
            {% endfor %}
          </ul>
        </div>
      </div>
    </div>
  </div>
</div>
